services:
  yakka-backend:
    build: .
    image: yakka-backend:latest
    container_name: yakka-backend

    # ports:
    #   - "8081:8080"                   # NEW
    network_mode: host

    env_file:
      - .env.dev
    environment:
      - PORT=8081                     # NEW (usar 8081 con host network)
      - DB_HOST=46.250.240.33
      - DB_PORT=5548
      - DB_USER=postgres
      - DB_PASSWORD=C0mpl3xSup3rP@ss!20392
      - DB_NAME=yakkadev
      - DB_SSLMODE=disable
      - ENVIRONMENT=development
      - LOG_LEVEL=warn

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8081/health"]  # NEW puerto correcto
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    # REMOVE: network_mode: host                # NEW (quitar esta línea si aún está)
